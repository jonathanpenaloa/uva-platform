{
  "name": "uva",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "npm-run-all --parallel dev:api dev:web",
    "dev:api": "npm --workspace @uva/api run dev",
    "dev:web": "npm --workspace @uva/web run dev"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
